# coffeeshop-menu Dockerfile (development — Vite dev server)
FROM node:20-alpine

WORKDIR /app

# Copy package files first for better caching
COPY package.json package-lock.json* ./
RUN npm install

# Copy source code
COPY . .

EXPOSE 5173

CMD ["npx", "vite", "--host", "0.0.0.0"]
