# Docker Compose for Coffeeshop Development
# Starts: PostgreSQL + API + Web Menu
# Note: POS (Wails desktop app) runs natively on the host — see README.md

services:
  # ─── PostgreSQL Database ───────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: coffeeshop-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: coffeeshop
      POSTGRES_PASSWORD: password
      POSTGRES_DB: coffeeshop
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U coffeeshop"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ─── Database Migrations ───────────────────────────────────────────
  migrate:
    image: migrate/migrate:v4.17.0
    container_name: coffeeshop-migrate
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./coffeeshop-api/migrations:/migrations
    command: >
      -path=/migrations
      -database=postgres://coffeeshop:password@postgres:5432/coffeeshop?sslmode=disable
      up
    # Runs once and exits

  # ─── Coffeeshop API ────────────────────────────────────────────────
  api:
    build:
      context: ./coffeeshop-api
      dockerfile: Dockerfile
    container_name: coffeeshop-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    environment:
      PORT: "8080"
      DATABASE_URL: postgres://coffeeshop:password@postgres:5432/coffeeshop?sslmode=disable
      JWT_SECRET: dev-secret-change-me
      ENVIRONMENT: development
      R2_ACCOUNT_ID: "3d229b6e907e0c45e10592aa6f50d488"
      R2_ACCESS_KEY_ID: "9cd056cd22f55518e0acda40476482c3"
      R2_SECRET_ACCESS_KEY: "b1334703e589dd323b482cc0edd10c906c64c3eaea9aafffbc801a12683dd749"
      R2_BUCKET_NAME: "cashier"
      R2_PUBLIC_URL: "https://pub-005956126efc4c20ad85fc718759c13e.r2.dev"
    ports:
      - "8080:8080"

  # ─── Web Menu (Vite Dev Server) ────────────────────────────────────
  menu:
    build:
      context: ./coffeeshop-menu
      dockerfile: Dockerfile.dev
    container_name: coffeeshop-menu
    restart: unless-stopped
    depends_on:
      - api
    environment:
      VITE_API_URL: http://localhost:8080
    ports:
      - "5173:5173"
    volumes:
      - ./coffeeshop-menu:/app          # Mount source for hot reload
      - /app/node_modules               # Keep container's node_modules

volumes:
  pgdata:
